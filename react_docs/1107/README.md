# 반복문 활용

리액트의 반복문은 리스트를 활용한다

중괄호 안에 리스트를 넣어 사용할 경우 

for문과 같은 별도의 반복문 없이도 화면에 출력되기 때문이다.

때문에 요소를 배열에 담을 때 사용되는 내장 함수를 사용한다

1. map
- 요소를 꺼내어 반복하며 개발자의 의해 mapping의 역할을 하여 하나의 리스트를 반환
- mapping: 짝을 맞춰준다 

2. filter
- boolean을 리턴하여 현재 값을 사용여부에 따른 리스트 반환
- 반복을 하면서 특정 조건하에 값을 리스트에 담아 반환