# DOM
parsing 뜯어본다
rendering 출력한다

도큐멘트를 오프젝트로 다루는 방법론

key - object(key) - object

웹 페이지를 동적으로 조작하기 위해 브라우저와 JS 사이에 중간다리 역할(인터페이스)

DOM 조작
html을 JS로 내용을 다루는 것

태그 우클릭 copy jh path


document.querySelector로 선택 후 내용을 바꾼다

### CSS 때 선택자를 떠올려 보자
1. 요소 선택
2. 스타일 추가
3. 얼마만큼

DOM 도 똑같다
1. 요소 선택
2. 어떤 Key
3. value

getElementByXXX
getElementsByXXX
옛날 방식

querySelector
요즘 방식

CSS class 많이 사용 JS id 많이 사용

아이유 이미지 링크를 사용하여
웹툰의 이미지를 바꾼다면
이것이 CSR이다

바깥의 태그선택자를 선택하면 내부까지 가져온다. 이것 조심하면 꼬이는 일이 덜하다